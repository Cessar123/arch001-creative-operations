# ARCH-001 Creative Operations — GitHub Export Guide

## ما الذي أصبح جاهزاً في الكود؟

هذه النسخة تبني غرفة عمليات عربية لعالم **ARCH-001 / Bashosha City**. الواجهة تعرض صورة الماستر، أحياء المدينة، سجل الشخصيات وحالات `DNA LOCK`، مهارات الإنتاج، وشاتاً ذكياً ينفذ بطاقات المشهد والحلقة والبرومو وسجل السكن والعمل.

## التشغيل داخل Manus

لا تحتاج إلى مفاتيح إضافية داخل المشروع الحالي. الشات يستخدم المحرك الخادمي المدمج، ولا تظهر أي مفاتيح في المتصفح. تسجيل الدخول وحفظ جلسات العمل يعملان ضمن البنية المدمجة للمشروع.

## التصدير إلى GitHub

من لوحة إدارة المشروع افتح **Settings → GitHub**، اختر المالك واسم المستودع، ثم صدّر الكود. بعد ذلك يصبح لديك مستودع React + Express + tRPC قابل للتطوير من أي مكان.

## تشغيل محلي بعد النسخ

```bash
pnpm install
pnpm dev
```

ثم افتح العنوان الذي يظهر في الطرفية. نفّذ التحقق قبل أي رفع:

```bash
pnpm check
pnpm test
pnpm build
```

## ملاحظة مهمة عن «المجاني» والشات الذكي

رفع **الكود** إلى GitHub مجاني. كما يمكن استضافة نسخة ثابتة من الواجهة على GitHub Pages مجاناً، لكن GitHub Pages لا يشغّل خادم Node ولا يحفظ مفتاح نموذج ذكاء اصطناعي بأمان؛ لذلك لا يستطيع تشغيل شات حي وحده.

| وضع النشر | خريطة الماستر والواجهة | الشات الذكي |
| --- | --- | --- |
| Manus الحالي | يعمل | يعمل عبر الخادم المدمج |
| GitHub Pages فقط | يعمل كواجهة/أرشيف مرئي | يحتاج خادماً خارجياً أو واجهة API |
| استضافة Node أو Serverless مع مفتاح محفوظ | يعمل | يعمل بعد توصيل مزود نموذج ذكاء اصطناعي |

لا تضع أي مفتاح API داخل `client/` أو في مستودع GitHub. أبقه داخل متغيرات بيئة في الخادم فقط. عندما تختار استضافة خارجية، استبدل موصل النموذج في `server/arch001Engine.ts` بموصل المزود الذي ستستخدمه، مع الحفاظ على `buildArch001SystemPrompt()` لأنه يحمل قواعد المدينة والـDNA.

## ملفات ARCH-001 المهمة

| المسار | دوره |
| --- | --- |
| `shared/arch001.ts` | سجل الشخصيات والأحياء والمهارات وحالات DNA LOCK. |
| `server/arch001Engine.ts` | سياق الشات الذكي وقواعد منع الانجراف. |
| `server/routers.ts` | مسار tRPC: `arch001.chat`. |
| `client/src/pages/Home.tsx` | واجهة غرفة العمليات. |
| `server/arch001Engine.test.ts` | اختبارات حارس الاستمرارية ومسار النموذج. |

## أمر اختبار الشات

```text
[001-DIR] ST-CAST-3D — CAST: شمندي، جوري، عيد — LOCATION: D02 — FUNCTION: مشكلة صباحية كوميدية.
```

النتيجة الصحيحة يجب أن تحافظ على شمندي كمرساة عائلية، وجوري كطفلة، وعيد كشخصية مقالب؛ ولا تبدّل وجوههم أو أعمارهم أو ملابسهم أو عائلاتهم.
